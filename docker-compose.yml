services:
  mediacleanup:
    build: .
    container_name: mediacleanup
    hostname: MEDIACLEANUP
    restart: unless-stopped
    volumes:
      - ./config.yml:/app/config.yml:ro
      - ./reports:/reports
      - ${MOVIES_ROOT:-/mnt/Movies}:/data/movies:ro
      - ${TVSHOWS_ROOT:-/mnt/TvShows}:/data/tvshows:ro
      - ${ANIME_ROOT:-/mnt/Anime}:/data/anime:ro
      - ${DOWNLOADS_ROOT:-/mnt/downloads}:/data/downloads:ro
    ports:
      - "${MEDIACLEANUP_PORT:-6996}:6996"
    networks:
      - default
      - media_stack
    environment:
      JELLYFIN_API_KEY: ${JELLYFIN_API_KEY:-}
      RADARR_API_KEY: ${RADARR_API_KEY:-}
      SONARR_API_KEY: ${SONARR_API_KEY:-}
      QBIT_USER: ${QBIT_USER:-}
      QBIT_PASS: ${QBIT_PASS:-}
    command: ["--serve", "--config", "/app/config.yml", "--output-dir", "/reports", "--port", "6996"]

networks:
  media_stack:
    external: true
    name: ${MEDIA_NETWORK:-proxy_network}
